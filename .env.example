# Nothing required yet for the phase-1 pipeline (Greenhouse/Lever/Ashby are public APIs,
# no keys needed). Added here so the shape is ready once we layer on Supabase + Resend + Stripe.

# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
# RESEND_API_KEY=
# STRIPE_SECRET_KEY=
